<!DOCTYPE html>
<html>
<head>
    <title>Home Page</title>
    <style>
        body {
        background-image: url(https://www.tokyo-odaiba.net/en/wp-content/uploads/sites/2/2018/11/popup_zone01.png);
        background-repeat: no-repeat;
        background-size: cover;
        }
        .nav {
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            border: 1px solid black;
            background-color: #f6f8fa;
            border-radius: 15px;
            justify-content: space-between;
        }
        .nav-item {
            text-transform: capitalize;
            color: black !important;
            padding: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .nav-item:first-child {
            margin-right: auto;
        }
        .form-container {
        background-color: white;
        border: 2px solid black;
        border-radius: 15px;
        padding: 20px;
        }
        .table-bordered th, .table-bordered td {
        border: 2px solid black;
        }
        .toy-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1em;
            padding: 1em;
        }
        .toy-item {
        border: 1px solid #333;
        padding: 1em;
        position: relative;
        border-radius: 15px; /*the corners rounded */
        box-shadow: 0 0 10px rgba(0,0,0,0.2); /*slight depth effect */
        background-color: #e5d3af;
        }
        .toy-item button {
            background-color: #4866ee; /* Green */
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 15px;
        }
        .in-stock {
        background-color: rgb(97, 229, 97);
        }

        .out-of-stock {
        background-color: rgb(226, 91, 91);
        }
        .footer {
            position: fixed;
            left: 0;
            bottom: 0;
            width: 100%;
            background-color: #0f0f0f;
            color: white;
            text-align: center;
            padding: 10px 0;
        }


    </style>
    
</head>
<body>
    <ul class="nav mt-3 mb-3">
        <li class="nav-item h2">
            <a class="nav-link" href="/home">Home</a>
        </li>
        
        <li class="nav-item h2">
            <a class="nav-link" href="/admin">ADMIN</a>
        </li>
    </ul>

    <div class="toy-grid">
    {{#each toys}}
    <div class="toy-item {{#if this.in_stock}}in-stock{{else}}out-of-stock{{/if}}">
        <img src="{{this.image}}" alt="{{this.name}}" width="100%">
        <h2>{{this.name}}</h2>
        <!-- Add a slash before the toy's ID in the URL -->
        <button onclick="window.location.href='toy/detail/{{this._id}}'">View Details</button>
        <p>{{this.description}}</p>
    </div>
    {{/each}}
</div>

<div class="footer">
    Contact us at <a href="mailto:info@example.com">leducanh@gmail.com</a>.
    </div>
</body>
</html>