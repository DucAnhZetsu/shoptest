<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            /* Replace 'background-image-url' with your actual URL */
            background-image: url('https://static.vecteezy.com/system/resources/previews/004/941/847/original/cosmetic-background-for-product-branding-and-packaging-presentation-geometry-form-circle-molding-on-podium-stage-with-shadow-of-leaf-background-design-eps10-vector.jpg');
            background-repeat: no-repeat;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .employee-image {
            display: block;
            margin-left: auto;
            margin-right: auto;
            border: 3px solid black; /* Adjust as needed */
        }
        .card {
            background-color: lightblue; /* Adjust as needed */
            padding: 20px;
            border-radius: 15px; /* Adjust as needed */
            box-shadow: 0 2px 5px rgba(0,0,0,0.3); /* Adjust as needed */
            width: 80%; /* Adjust as needed */
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid black;
            padding: 10px;
            text-align: left;
        }
        .back-button {
            display: block;
            width: 100px;
            height: 50px;
            margin: 20px auto;
            background-color: #4CAF50; /* Green */
            color: white;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
        }
    </style>
</head>
<body>
   <div class="card">
      <!-- Add an image at the top of the card -->
      <img class="employee-image" src="{{employee.image}}" width="300" height="300" alt="this is employee image">
      <h1 class="text-center bg-success">Thông tin</h1>
      <table>
          <tr><th>Họ tên:</th><td>{{ employee.name }}</td></tr>
          <tr><th>Tuoi:</th><td>{{ employee.age }}</td></tr>
          <tr><th>Ngày sinh:</th><td>{{ dateFormat employee.dob "DD-MM-YYYY"}}</td></tr>
          <tr><th>Điểm GPA:</th><td>{{ employee.gpa }}</td></tr>
          <tr><th>Giới tính:</th><td>{{ employee.gender }}</td></tr>
          <tr><th>Email:</th><td>{{employee.email}}</td></tr>
      </table>
      <button class="back-button" onclick="window.history.back();">Back</button>
   </div>
</body>
</html>
